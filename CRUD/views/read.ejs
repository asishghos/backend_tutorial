<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Read User</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 text-white flex flex-col items-center p-6">
    <h1 class="text-2xl font-bold mb-6">User Details</h1>
    
    <div class="grid grid-cols-2 gap-4 w-full max-w-4xl">
        <% users.forEach(function(user) { %>
            <div class="bg-gray-800 p-4 rounded shadow-md flex flex-col items-center text-center">
                <p class="text-lg font-semibold text-blue-400"><%= user.username %></p>
                <p class="text-sm text-green-400"><%= user.email %></p>
                <p class="text-sm text-yellow-400 font-medium"><%= user.role %></p>
                <div class="mt-4 flex space-x-2">
                    <a href="/update/<%= user.id %>" class="bg-blue-500 text-white px-3 py-1 rounded hover:bg-blue-600">Edit</a>
                    <a href="/delete/<%= user.id %>" class="bg-red-500 text-white px-3 py-1 rounded hover:bg-red-600">Delete</a>
                </div>
            </div>
        <% }); %>
    </div>
    
    <a href="/" class="mt-6 text-blue-500 hover:underline text-lg">Back to home</a>
    
    <script src="/javascripts/script.js"></script>
</body>

</html>